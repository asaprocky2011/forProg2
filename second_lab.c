#include <stdio.h>

int main() 
{
    // uint1024_t a;
    // uint1024_t b, v;
    // scanf_value(&a);
    // scanf_value(&b);
    // printf("multiplication: ");
    // v = mult_op(a, b);
    // printf_value(mult_op(a, b));
    // printf("\ndifference: ");
    // printf_value(subtr_op(a, b));
    // printf("\namount: ");
    // printf_value(add_op(a, b));
    return 0;
}

